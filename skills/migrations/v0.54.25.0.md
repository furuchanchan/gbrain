# v0.54.25.0: managed-root enrichment preflight

No upgrade action is required. Existing enrichment jobs, checkpoints, and
failure records are preserved unchanged. Enrichment that was misreported as a
provider failure surfaces a typed `writer_coordinator_required` reason from
this version forward; complete writer activation on the claimed root rather
than deleting markers to unblock it.
