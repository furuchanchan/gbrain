# v0.54.12.0

- `test/e2e/put-page-persistence-postgres.test.ts` no longer asserts the
  fixture's worktree binding is non-null immediately after a committed
  receipt: it polls `getWorktreeBinding` for up to five seconds first,
  closing the registration-visibility race that intermittently failed
  unrelated PRs' CI. Test-only change — no runtime behaviour.
