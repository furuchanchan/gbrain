# v0.57.0.17

- `gbrain providers env <id>` prints `Base URL: <url> (<source>)` (#5302) resolved through file/env/default planes only — openai-compat: file-plane `provider_base_urls` > known `*_BASE_URL` env > recipe default; native: env > file plane. A DB-plane `provider_base_urls.<id>` set via `gbrain config set` applies at runtime when no file-plane value exists, but this command does not read it — the output names that scope and points DB-plane verification at `gbrain config get provider_base_urls.<id>`. The mistral recipe comment now notes the default endpoint is EU-by-default (not EU-assured), names `https://api.eu.mistral.ai/v1` for regional inference, and documents the same verification path. No action needed.
