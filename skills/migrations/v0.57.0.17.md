# v0.57.0.17

- `gbrain providers env <id>` prints `Base URL: <url> (<source>)` (#5302) resolved through the real precedence — openai-compat: file-plane `provider_base_urls` > known `*_BASE_URL` env > recipe default; native: env > file plane — plus the `gbrain config set provider_base_urls.<id>` override. A DB-plane value set via `config set` applies at runtime when no file-plane value exists. The mistral recipe comment now notes the default endpoint is EU-by-default (not EU-assured) and names `https://api.eu.mistral.ai/v1` for regional inference. No action needed.
