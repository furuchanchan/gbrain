# v0.54.7.0

- Idle `gbrain serve` on a managed brain no longer issues a `chmod` + directory `fsync` per persistence tick (#5297): mode enforcement only repairs drift, and the registry fsync only runs when a record was rewritten. Behavior is unchanged apart from the removed syscalls. No action needed.
