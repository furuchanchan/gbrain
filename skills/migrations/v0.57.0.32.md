# v0.57.0.32

- `extract_facts` envelopes now carry `job_binding` (#5278): `{recorded: true, fact_ids, recorded_at}` on success, `{recorded: false, reason}` on every short-circuit (kill-switch, dream_generated, extraction_unavailable, extraction_failed). Callers enforcing a strict binding contract can rely on the field; its absence marks an engine that predates the contract. No action needed.
