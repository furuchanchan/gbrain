# v0.52.40.0

- `gbrain embed --stale` names skipped pages instead of silently no-opping (#5289): the three quiet early-returns in the stale drain (no sealed projection snapshot, projection `chunk_text` divergence, `installPageEmbeddings` refusal) now record failures with reasons, and a run that selected stale chunks but embedded zero prints a backlog-unchanged WARNING. A run that still shows a stuck backlog now tells you which pages and why. No action needed.
