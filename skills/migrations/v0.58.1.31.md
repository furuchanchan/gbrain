# v0.58.1.31

- Legacy DB-only backstop inserts now probe the natural key (source_id, entity_slug, fact, kind, context, valid_from) before inserting; a live identical row counts as `duplicate` and is skipped, so re-extraction no longer writes exact duplicates (#5275). Applies to both legacy paths (unparented bucket + stub-guard fallback). No action needed.
